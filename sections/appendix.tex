\section{Additional Method Details}
\label{app:method-details}

This section gives the formal episode interface, memory representation, baseline
specification, and implementation details that support the method described in
the main paper.

\subsection{Formal Episode Interface}
\label{app:episode}

Each navigation episode references one causal prediction query. Public fields include schema version, episode and query identifiers, split, task type, scene and household identifiers, world variant, target description, query time, agent start pose, candidate-state identifiers, budgets, and success criteria. Evaluation-private fields include the current state, target pose, valid goal viewpoints, hidden transition class, oracle path, mobility label, and query-time activity. Train and validation releases may expose analysis labels in metadata; the formal test release replaces them with null values and computes grouped metrics in a private evaluator.

The high-level track exposes
\begin{center}
\texttt{NAVIGATE\_TO(state)}, \texttt{INSPECT(state)}, \texttt{STOP}, \texttt{NOT\_FOUND},
\end{center}
with a fixed navigation backend. The low-level track exposes Habitat-style forward, turn, look, and stop actions. The main paper uses the high-level track to isolate predictive state selection; low-level results measure sensitivity to control and perception.

\subsection{Detailed 4D Memory Construction}
\label{app:memory-details}

\Cref{fig:memory} illustrates how observations are converted into persistent
entity versions. Each observation contributes spatial evidence and temporal
validity, allowing the memory to preserve both previous and current states.
Memory is materialized
causally from versioned deltas:
\begin{equation}
\mathcal M_{\leq t}=\operatorname{Materialize}(\Delta\mathcal M_0,\ldots,\Delta\mathcal M_t)
=(\mathcal G_t,\mathcal V_t,\mathcal B_t),
\label{eq:memory}
\end{equation}
where $\mathcal G_t$ stores semantic--spatial relations, $\mathcal V_t$ stores
time-valid entity versions, and $\mathcal B_t$ stores observation provenance.
Persistent identity is separated from mutable state: a change appends a
time-valid version without erasing earlier states, and each memory query
uses only evidence available at its decision time.

For image coordinate
$\widetilde{\mathbf u}=[u,v,1]^\top$ with depth $D_t(u,v)$, the corresponding
world point is
\begin{equation}
\mathbf x^w=\operatorname{proj}_3\!\left(
\mathbf T_{w\leftarrow c,t}
\begin{bmatrix}D_t(u,v)\mathbf K^{-1}\widetilde{\mathbf u}\\1\end{bmatrix}
\right),
\label{eq:backprojection}
\end{equation}
where $\mathbf K$ is the camera intrinsic matrix and $\mathbf T_{w\leftarrow c,t}$ is the camera-to-world transform. Cross-view association uses semantic compatibility, 3D overlap, temporal consistency, and source confidence. The $m$-th version of entity $o$ is
\begin{equation}
h_o^m=([t_{o,s}^m,t_{o,e}^m),s_o^m,c_o^m,f_o^m,\mathcal P_o^m),
\end{equation}
containing a validity interval, candidate state, confidence, visual feature, and evidence handles. A state change appends a version without erasing the earlier trajectory, preserving both causal history and observation provenance.

\begin{figure}[!t]
    \centering
    \includegraphics[width=\linewidth]{figures/EvolvingWorldNav.pdf}
    \caption{Temporally versioned 4D memory construction.}
    \label{fig:memory}
\end{figure}

\subsection{Belief Encoding and Training}
\label{app:belief-encoding}

For target $o$, the retrieved history contains $K$ causal events,
\begin{equation}
H_o=\{e_k=(o_k,s_k,t_k,z_k,c_k,v_k)\}_{k=1}^{K},
\label{eq:history}
\end{equation}
with entity, state, time, evidence type, confidence, and visibility as defined
in \cref{sec:belief_model}. Each event is encoded as
\begin{align}
\mathbf x_k={}&E_o(o_k)+E_s(s_k)+E_z(z_k)+E_c(c_k)+E_v(v_k)\nonumber\\
&+\phi_{\Delta}(t_q-t_k)+\phi_{\mathrm{cal}}(t_k),
\label{eq:eventtoken}
\end{align}
where $\phi_{\Delta}$ includes $\log(1+t_q-t_k)$ and
$\phi_{\mathrm{cal}}$ contains sinusoidal hour-of-day and day-of-week features.
A learned query token specifies the target, query time, and last positive state.
The history encoder produces
\begin{equation}
(\mathbf h_1,\ldots,\mathbf h_K,\mathbf h_q)
=\operatorname{CTTransformer}(\mathbf x_1,\ldots,\mathbf x_K,\mathbf x_q).
\end{equation}
The persistence head estimates agreement between the current state and the
last positive observation:
\begin{equation}
\rho_q=P(s_{t_q}^o=s_{\mathrm{last}}\mid H_o,t_q)
=\sigma(\mathbf w_{\rho}^{\top}\mathbf h_q).
\label{eq:persistence}
\end{equation}
For each alternative candidate
$i\in\widetilde{\mathcal C}_o\setminus\{s_{\mathrm{last}}\}$,
including $\mathrm{unknown}$, a shared pointer head computes
\begin{equation}
a_i=\frac{(W_q\mathbf h_q)^\top(W_c\mathbf g_i)}{\sqrt d}
+\psi(\mathbf h_q,\mathbf g_i),\qquad
\pi_q(i)=\frac{\exp a_i}
{\sum_{u\in\widetilde{\mathcal C}_o\setminus\{s_{\mathrm{last}}\}}\exp a_u},
\label{eq:pointer}
\end{equation}
where $\mathbf g_i$ represents candidate context, functional role, geometry,
and instance identity, $d$ is the projection dimension, and $\psi$ is a
learned compatibility function. The heads jointly define the normalized belief
in \cref{eq:prior} and are trained with current-state negative log-likelihood,
$\mathcal L_{\mathrm{state}}=-\log b_q^-(s_{t_q}^o)$, without a separate
change label or oracle mobility type.

\subsection{Full Baseline Specification}
\label{app:baselines}

Last Seen assigns all mass to the latest positive state. Frequency Prior estimates $P(s\mid o)$ from training data. Markov Transition estimates $P(s_{k+1}\mid s_k,o)$; Time-Conditioned Prior additionally conditions on public hour and weekday bins. Instance Hotspot uses only the target instance's training trajectory. GRU Direct and Transformer Direct receive the same event tokens and candidate encoder as \pfdnav{} but directly normalize candidate logits without the persistence factorization. P4D-Belief Prior uses \cref{eq:prior} once at episode start. Full \pfdnav{} applies \cref{eq:bayes} and \cref{eq:planner} after each inspection.

The Oracle Activity diagnostic may use the hidden activity and source location but is excluded from fair rankings. Oracle Current State receives the private target state and measures remaining perception and navigation error. External predictive methods are adapted only through their public inputs; any use of privileged simulator state must be identified and excluded from the main comparison.

\subsection{Implementation Details}
\label{app:implementation}

Unless otherwise noted, the history encoder has three Transformer layers, hidden width $128$, four attention heads, and at most 64 events. The predictive model contains approximately \na million trainable parameters and is optimized with AdamW using validation NLL for checkpoint selection. Candidate encoders are shared across scenes. The main high-level track fixes candidate viewpoints and low-level planning; a Habitat low-level action track evaluates the complete embodied stack. Final learning rate, batch size, epoch count, hardware, simulator version, detector, visibility thresholds, and repeated-run protocol will be frozen before test evaluation.

\subsection{Experimental Details}
\label{app:experimental-setup}

\paragraph{Benchmark protocols.}
The complete agent is evaluated on \pfdbench{}, FindingDory, and GOAT-Bench
under their native task definitions. FindingDory reports high-level goal
selection and conditional low-level execution; GOAT-Bench reports official SR
and SPL, with Repeat-SR used only as a marked memory-reuse diagnostic. The
cross-domain matrix uses R2R-CE on MP3D, HM3D-OVON on HM3D, native PointNav on
Habitat-GS, SAGE-Bench on InteriorGS, and \pfdbench{} on HSSD. Scores are never
pooled across these heterogeneous tasks, and published values are transferred
only when split, sensors, actions, and success rules match.

\paragraph{Prediction splits.}
The prediction-only study trains on days 0--29, selects configurations on days
30--34, and evaluates on days 35--39 of five HSSD histories
(5,475/910/915 queries). Temporal evaluation pools the scenes and reports 322
changed-only queries; leave-one-scene-out evaluation trains on four scenes and
tests on the fifth using 644 change-balanced queries. All predictors share query
identities, causal histories, and legal candidate sets. The main table reports
five-seed variation; no future observation or private transition label is
available at inference time.

\paragraph{Real-world protocol.}
LYNX M20 is the primary quantitative platform, with X30 and Lite3 used for
transfer. Each method receives identical pre-query history, candidate locations,
start condition, perception interface, and robot-specific low-level stack.
Targets remain stationary during each 360-second search episode, and success
requires autonomous online confirmation within three inspections. Missed
detections, navigation failures, timeouts, and human interventions count as
failures. Indoor/outdoor balance, platform specifications, and condition-wise
breakdowns are provided in \cref{app:real-world}.

\paragraph{Reporting controls.}
Within a comparison, methods share detectors, candidate states, inspection
viewpoints, and budgets. Released navigation agents retain their native policy;
memory-only and prediction-only systems are attached to the shared planner so
that temporal reasoning is not confounded with a stronger perception or control
stack. Results are stratified by world variant, staleness, transition class,
mobility regime, and generalization split. Paired routine/static/random episodes
retain scene, query, and marginal placement factors while changing only the
hidden transition mechanism.

\section{Benchmark Construction and Evaluation Protocol}
\label{app:benchmark-protocol}

This section documents episode validity, leakage controls, mobility assignment,
and the metrics used for all reported comparisons.

\input{sections/appendix_benchmark}

\subsection{Episode Validity and Leakage Audit}
\label{app:audit}

An episode is retained only if: (i) the target has at least one positive pre-query observation; (ii) the target state is known to the private evaluator and physically instantiated without collision; (iii) at least one valid viewpoint lies on the NavMesh; (iv) the start is on the NavMesh, at least $3$ meters from the target, and does not reveal it; and (v) all public histories terminate at or before the query. A successful stop must be within $1$ meter geodesic distance of a valid viewpoint, observe the target above a frozen visible-fraction threshold, and identify the correct instance or category.

We audit serialized examples for future timestamps, hidden event fields, filenames or indices correlated with answers, duplicate event groups across splits, and inconsistent paired variants. Static, routine, and random members of a pair must have identical public query fields. Invalid simulator episodes are reported separately and are not counted as ordinary failures.

\subsection{Mobility-Type Assignment}
\label{app:mobility}

The frozen rule is
\begin{equation}
\operatorname{type}(o)=
\begin{cases}
\mathrm{stable}, & r_{\mathrm{move}}(o)<\tau_{\mathrm{move}},\\
\mathrm{activity}, & G_{\mathrm{act}}(o)\geq\tau_{\mathrm{act}},\\
\mathrm{personal}, & C_{\mathrm{habit}}(o)\geq\tau_{\mathrm{habit}},\\
\mathrm{irregular}, & \text{otherwise}.
\end{cases}
\end{equation}
All thresholds are selected on training data before navigation evaluation. When the generator specifies a mobility profile, the same statistics are used to verify that realized trajectories exhibit the intended behavior. Every group must cover several categories, instances, and scenes, and no category may uniquely reveal one mobility type.

\subsection{Metric Definitions}
\label{app:metrics}

Let $S_i$ be episode success, $L_i$ the executed path, and $L_i^*$ the oracle shortest path to the query-time goal under the same start. Standard SPL is
\begin{equation}
\mathrm{SPL}=\frac{1}{N}\sum_{i=1}^{N}S_i\frac{L_i^*}{\max(L_i,L_i^*)}.
\end{equation}
First-Go SR counts success when only the initial predicted candidate may be inspected. Recovery SR conditions on an incorrect first candidate and measures whether the target is subsequently found. Excess path is $L_i-L_i^*$ for static-after-query episodes. For online-dynamic episodes, the oracle is trajectory aware and may intercept the target; we therefore report both success within the common budget and $L_i-L_{i,\mathrm{dyn}}^*$.

Calibration metrics operate on the full candidate belief before navigation and after each update. ECE bins maximum confidence, Brier score evaluates all candidates, and true-state rank avoids hiding useful changes below Top-1. Search-cost curves plot success against allowed inspections and travelled distance rather than reporting one arbitrary budget.

\section{Additional Experimental Results}
\label{app:experimental-results}

This section provides extended quantitative results omitted from the main paper
for space constraints.

\input{sections/appendix_results}

\subsection{P4D-Bench Detailed Results}
\label{app:moved-tables}

Table~\ref{tab:mainnav} complements the main benchmark table with search cost
and recovery after an incorrect first destination; lower is better for the two
cost columns. Table~\ref{tab:foundation_models} tests whether paired navigation
gains persist across different VLMs under matched tools, prompts, episodes, and
action budgets.

\begin{table}[!t]
\caption{Search cost and recovery on P4D-Bench routine worlds.}
\label{tab:mainnav}
\centering
\scriptsize
\setlength{\tabcolsep}{8.0pt}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Inspect. $\downarrow$} &
\makecell{\textbf{Distance (m)}\\\textbf{$\downarrow$}} &
\makecell{\textbf{Recovery SR (\%)}\\\textbf{$\uparrow$}}\\
\midrule
\rowcolor{tblnav}\multicolumn{4}{c}{\emph{Navigation and persistent memory}}\\
VLFM \citep{yokoyama2024vlfm} & 4.85 & 18.72 & 22.41\\
LagMemo GLUE \citep{zhou2025lagmemo} & 3.92 & 13.24 & 45.62\\
HOV-SG \citep{werby2024hovsg} & 3.36 & 12.08 & 51.24\\
DynaMem \citep{liu2024dynamem} & 2.51 & 11.59 & 63.93\\
\midrule
\rowcolor{tblpredict}\multicolumn{4}{c}{\emph{Predictive world and state modeling}}\\
SLaTe-PRO \citep{patel2023routine} & 4.40 & 14.58 & 29.00\\
SGM+NEP \citep{kurenkov2023dynamic} & 4.08 & \textbf{7.42} & 27.56\\
FlowMaps \citep{argenziano2026flowmaps} & 4.66 & 16.51 & 20.27\\
Perpetua\textsuperscript{*} \citep{saavedraruiz2026predictivegraphs} & 3.71 & 10.48 & 17.96\\
\midrule
\rowcolor{tblours}\textbf{\evolvingworldnav{} (ours)} & \textbf{2.18} & 8.36 & \textbf{68.42}\\
\bottomrule
\multicolumn{4}{l}{\footnotesize \textsuperscript{*}Released upstream predictor evaluated in the shared navigation stack.}
\end{tabular}
\end{table}

\evolvingworldnav{} requires the fewest inspections (2.18) and achieves the
highest Recovery SR (68.42\%), improving over DynaMem by 4.49 points after an
incorrect first destination. SGM+NEP travels the shortest average distance
(7.42\,m), while \evolvingworldnav{} remains close at 8.36\,m. Together with
the First-Go, Search SR, and SPL results in \cref{tab:crossbench}, these
complementary metrics show that the gain is not only higher eventual success:
retained uncertainty and evidence updating reduce redundant inspections and
support recovery when the initial prediction is wrong.

\begin{table}[!t]
\caption{VLM robustness on P4D-Bench; SR is in percent and SPL is unscaled.}
\label{tab:foundation_models}
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{@{}lccccccc@{}}
\toprule
\multirow{2}{*}{\textbf{Different VLMs}} &
\multicolumn{3}{c}{\textbf{Last-seen Agent}} &
\multicolumn{3}{c}{\textbf{\evolvingworldnav{}}} &
\multirow{2}{*}{\makecell{\textbf{$\Delta$ Search}\\\textbf{SR $\uparrow$}}}\\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
& \makecell{First-Go\\SR $\uparrow$} & \makecell{Search\\SR $\uparrow$} &
SPL $\uparrow$ & \makecell{First-Go\\SR $\uparrow$} &
\makecell{Search\\SR $\uparrow$} & SPL $\uparrow$ & \\
\midrule
GPT-4o & 31.88 & 48.94 & 0.3215 & \textbf{59.72} & \textbf{71.86} & \textbf{0.6027} & \textbf{+22.92}\\
GPT-5.5 & 42.36 & 51.31 & 0.3732 & \textbf{63.48} & \textbf{73.57} & \textbf{0.6531} & \textbf{+22.26}\\
GPT-5.6-Luna & 46.92 & 57.43 & 0.3970 & \textbf{63.87} & \textbf{83.36} & \textbf{0.6639} & \textbf{+25.93}\\
Qwen2.5-VL-3B & 16.26 & 26.55 & 0.1502 & \textbf{38.27} & \textbf{45.02} & \textbf{0.3727} & \textbf{+18.47}\\
Qwen2.5-VL-32B & 18.84 & 31.30 & 0.1591 & \textbf{42.44} & \textbf{46.21} & \textbf{0.3713} & \textbf{+14.91}\\
\midrule
\multicolumn{7}{r}{\textbf{Average gain}} & \textbf{+20.90}\\
\bottomrule
\end{tabular}
\end{table}

\FloatBarrier
\subsection{Fine-Grained Ablation Analysis}
\label{app:fine-grained-ablation}
\label{app:prediction-evidence}

The main-paper ablation isolates the five decisions that define the complete
agent. Here we decompose implementation choices within temporal memory,
evidence updating, and the predictor while preserving the same perception,
candidate set, inspection viewpoints, and low-level navigator.

\subsubsection{Temporal and Memory Ablations}

\begin{table}[!htbp]
\caption{Temporal and memory ablations; SR and SPL are percentages.}
\label{tab:temporal-memory-ablation}
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\begin{tabularx}{0.88\linewidth}{@{}l>{\raggedright\arraybackslash}Xcccc@{}}
\toprule
\textbf{Group} & \textbf{Variant} &
\makecell{\textbf{First-Go}\\\textbf{SR $\uparrow$}} &
\makecell{\textbf{Search}\\\textbf{SR $\uparrow$}} & \textbf{SPL $\uparrow$} &
\textbf{Inspect. $\downarrow$}\\
\midrule
\multirow{3}{*}{Temporal encoding}
& w/o elapsed-time encoding & 48.37 & 80.28 & 64.82 & 4.36\\
& w/o calendar context & 47.75 & 76.11 & 66.20 & 4.05\\
& w/o both temporal cues & 40.84 & 76.03 & 64.98 & 4.12\\
\midrule
\multirow{2}{*}{Historical evidence}
& w/o instance-specific history & 57.57 & 80.77 & 66.61 & 4.30\\
& w/o negative history & 45.86 & 79.53 & 63.83 & 4.44\\
\midrule
& $K=8$ & 39.55 & 63.77 & 56.00 & 4.57\\
& $K=16$ & 45.88 & 69.48 & 65.77 & 4.37\\
& $K=32$ & 56.25 & 79.60 & 68.40 & 4.15\\
\rowcolor{tblours}\multirow{-4}{*}{History capacity} & \textbf{$K=64$ (Full)} & \textbf{60.23} &
\textbf{84.63} & \textbf{70.34} & \textbf{3.98}\\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

Elapsed-time and calendar ablations distinguish irregular observation gaps from
daily and weekly periodic context. Removing both cues reduces First-Go SR by
19.39 points relative to the full model. Instance-specific trajectories provide
smaller but consistent gains, whereas removing negative history costs 14.37
First-Go points and 6.51 SPL points. Increasing capacity from $K=8$ to $K=64$
raises First-Go SR by 20.68 points, Search SR by 20.86 points, and SPL by 14.34
points while reducing the average inspection count from 4.57 to 3.98. These
trends show that performance benefits from temporally structured evidence over a
sufficiently long history, rather than merely the most recent observations.

\subsubsection{Evidence Update Ablations}

\Cref{fig:agent-evidence-update} illustrates the update applied after an
unsuccessful inspection. Causal memory initially assigns probability 0.62 to the
last-seen coffee table. When a reliable detector ($c=0.95$) fails to observe the
target from a high-coverage view ($v=0.98$), the likelihood at that state falls
to 0.069. The posterior consequently shifts from $(0.62,0.25,0.13)$ to
$(0.10,0.59,0.31)$ and the planner selects the kitchen island next. The inspected
state retains nonzero mass, reflecting residual perceptual uncertainty rather
than an irreversible deletion.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{figures/Agent_Evidence_Update.pdf}
\caption{Evidence-aware belief updating and replanning after a negative inspection.}
\label{fig:agent-evidence-update}
\end{figure}

\begin{table}[!htbp]
\caption{Belief quality after negative-evidence updates; Top-1 is a percentage.}
\label{tab:evidence-update-ablation}
\centering
\scriptsize
\setlength{\tabcolsep}{8pt}
\begin{tabular}{lcccc}
\toprule
\textbf{Update rule} & \textbf{Rank $\downarrow$} & \textbf{Top-1 $\uparrow$} &
\textbf{NLL $\downarrow$} & \textbf{ECE $\downarrow$}\\
\midrule
No Update & 5.82 & 62.98 & 2.838 & 0.172\\
Hard Removal & 10.09 & 42.69 & 11.514 & 0.204\\
Bayesian Update & 5.25 & 64.71 & \textbf{1.497} & 0.151\\
\rowcolor{tblours}\textbf{Bayesian + Visibility (Ours)} &
\textbf{4.96} & \textbf{66.93} & 1.530 & \textbf{0.130}\\
\bottomrule
\end{tabular}
\end{table}

No Update leaves the prior unchanged, while Hard Removal assigns zero mass after
every unsuccessful inspection. This irreversible rule performs worst across all
four metrics, showing that an apparent miss is insufficient evidence when the
view is partially occluded or perception is uncertain. Bayesian updating lowers
NLL from 2.838 to 1.497 without discarding hypotheses outright. Adding
visibility produces the best mean rank (4.96), Top-1 accuracy (66.93\%), and ECE
(0.130), while its NLL remains within 0.033 of the uncalibrated Bayesian update.
The result supports viewpoint-conditioned negative evidence as the most reliable
input to the observe--update--replan loop.

\subsubsection{Predictor Architecture}

\begin{table}[!htbp]
\caption{Predictor architecture on temporal changed-only queries.}
\label{tab:predictor-architecture-ablation}
\centering
\scriptsize
\setlength{\tabcolsep}{7pt}
\begin{tabular}{lccc}
\toprule
\textbf{Predictor} & \textbf{Top-1 $\uparrow$} & \textbf{MRR $\uparrow$} &
\textbf{NLL $\downarrow$}\\
\midrule
GRU & 32.61 & 0.5471 & 1.8279\\
Direct Transformer & 34.16 & 0.5586 & 1.7292\\
\rowcolor{tblours}\textbf{Structured Transformer (ours)} &
$\mathbf{38.24\pm1.42}$ & $\mathbf{0.5971\pm0.0291}$ &
$\mathbf{1.4925\pm0.0867}$\\
\bottomrule
\end{tabular}
\end{table}

All three predictors use the same observation tokens and candidate encoder.
The Direct Transformer normalizes candidate logits directly, whereas the
structured model separates persistence from relocation. This controlled view
therefore isolates architectural factorization from memory content and action
selection.

\subsection{Evolution-Aware Diagnostics}
\label{app:evolution-diagnostics}

\par\medskip


\paragraph{Paired-world control.}
\Cref{tab:worlds} holds scenes and queries fixed while changing only the world
transition mechanism. Last Seen reaches 100.00 SR in static worlds but falls to
38.49 under routine evolution, exposing the failure of stale memory. In contrast,
\pfdnav{} reaches 60.85 routine-world SR, improving by 22.36 points over Last
Seen and by 12.03 points over Transformer Direct. Its lower random-world result
(30.12 SR) further localizes this advantage to learnable temporal regularity
rather than a scene-frequency or generic search shortcut.

\paragraph{Effect of memory staleness and inspection budget.}
\Cref{fig:predictive-memory} complements these aggregate tables by showing
how performance changes with temporal staleness, inspection budget, and hidden
mobility type. These episode-level diagnostics are not inferred from aggregate
success rates.

\FloatBarrier
\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/predictive-memory.pdf}
\caption{Predictive diagnostics under memory staleness and search budgets.}
\label{fig:predictive-memory}
\end{figure}

\FloatBarrier

\section{Real-World Evaluation}
\label{app:real-world-eval}

This section reports the physical evaluation, first describing the robot
platforms and then disaggregating the measured M20 results by environment,
temporal condition, and qualitative case.

\subsection{Robot Platforms}
\label{app:robot-platforms}

We conduct the real-world evaluation using three commercial robotic platforms
from DEEP Robotics: LYNX M20, X30, and Lite3. Table~\ref{tab:robot-platforms}
summarizes the platforms used in the physical evaluation. Platform-specific
perception and low-level navigation are abstracted behind a common interface,
while the \evolvingworldnav{} memory, prediction, belief-update, and high-level
search components remain unchanged.

\begin{table}[!t]
\caption{Robot platforms used in real-world evaluation.}
\label{tab:robot-platforms}
\centering
\scriptsize
\setlength{\tabcolsep}{8pt}
\begin{tabular}{lllll}
\toprule
Platform & Morphology & Standing size & Mass & Endurance / range \\
\midrule
LYNX M20 & Wheel-legged & $820\times430\times570$ mm & $\sim$35 kg & 3 h / 15 km unloaded \\
X30 & Quadruped & $1000\times695\times470$ mm & 56 kg & 2.5--4 h / $\geq$10 km \\
Lite3 (LiDAR) & Quadruped & $610\times370\times496$ mm & 13.5 kg & 1.5--2 h / 2.7 km \\
\bottomrule
\end{tabular}
\end{table}

The reported endurance and range values are manufacturer laboratory specifications and may differ from those obtained under the indoor and outdoor conditions used in our evaluation. We therefore do not use manufacturer-rated mobility or endurance as experimental outcomes. Absolute completion time is compared only between methods executed on the same robot platform. The M20 uses dual 96-line LiDARs and wide-angle cameras; X30 provides integrated perception and autonomous navigation; and the Lite3 LiDAR configuration exposes obstacle avoidance and autonomous navigation through its platform interface.

\subsection{Environment Breakdown}
\label{app:real-world}

Table~\ref{tab:real-domain} separates indoor and outdoor trials. The outdoor
setting has longer routes and lower success for all methods, while the direction
of the \evolvingworldnav{} gain is consistent across both environments.

\begin{table}[!t]
\caption{M20 results by environment.}
\label{tab:real-domain}
\centering
\scriptsize
\setlength{\tabcolsep}{5pt}
\begin{tabular}{lrrrrrr}
\toprule
& \multicolumn{3}{c}{Indoor} & \multicolumn{3}{c}{Outdoor} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
Method & First-Go & Search SR & Dist. & First-Go & Search SR & Dist. \\
\midrule
Last Seen + Search & 18.8 & 31.3 & 28.6 & 15.6 & 21.9 & 83.8 \\
Time Frequency & 25.0 & 34.4 & 27.2 & 18.8 & 28.1 & 78.6 \\
Retrieval + Reasoning & 31.3 & 40.6 & 25.1 & 21.9 & 34.4 & 75.5 \\
\textbf{\evolvingworldnav{}} & \textbf{40.6} & \textbf{53.1} & \textbf{22.4}
& \textbf{28.1} & \textbf{43.8} & \textbf{65.2} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Temporal Condition Analysis}
\label{app:temporal-analysis}

\begin{wraptable}{r}{0.60\linewidth}
\vspace{-0.7\baselineskip}
\centering
\caption{M20 Search SR (\%) by temporal condition.}
\label{tab:real-condition}
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Method & Unchanged & \makecell{Routine-\\consistent} &
\makecell{Weak-\\routine} & \makecell{Broken-\\routine} \\
\midrule
Last Seen + Search & \textbf{68.8} & 18.8 & 12.5 & 6.3 \\
Time Frequency & 50.0 & 50.0 & 18.8 & 6.3 \\
Retrieval + Reasoning & 62.5 & 43.8 & 25.0 & 18.8 \\
\textbf{\evolvingworldnav{}} & 62.5 & \textbf{68.8} & \textbf{37.5} & \textbf{25.0} \\
\bottomrule
\end{tabular}
\end{wraptable}

\paragraph{Temporal conditions.}
\Cref{tab:real-condition} shows that predictive memory is most useful when
recurring changes provide a usable historical signal. Last Seen remains strong
when nothing changes, whereas all methods degrade as regularity weakens. The
condition-wise view separates benefits from routine modeling from improvements
that could be explained by generic search alone.

\par\medskip
\begin{wraptable}[7]{r}{0.60\linewidth}
\vspace{-0.7\baselineskip}
\centering
\caption{Cross-platform transfer results.}
\label{tab:real-transfer}
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{@{}llrrr@{}}
\toprule
Platform & Method & First-Go SR & Search SR & Dist. (m) \\
\midrule
\multirow{2}{*}{LYNX M20} & Last Seen + Search & 18.8 & 25.0 & 55.4 \\
& \textbf{\evolvingworldnav{}} & \textbf{37.5} & \textbf{50.0} & \textbf{44.6} \\
\midrule
\multirow{2}{*}{X30} & Last Seen + Search & 18.8 & 25.0 & 54.1 \\
& \textbf{\evolvingworldnav{}} & \textbf{31.3} & \textbf{43.8} & \textbf{46.8} \\
\midrule
\multirow{2}{*}{Lite3} & Last Seen + Search & 18.8 & 31.3 & 51.8 \\
& \textbf{\evolvingworldnav{}} & \textbf{25.0} & \textbf{43.8} & \textbf{45.9} \\
\bottomrule
\end{tabular}
\end{wraptable}

\paragraph{Recovery and cross-platform transfer.}
After failed first inspections, \evolvingworldnav{} recovered 9/37 M20
episodes (24.3\%). \Cref{tab:real-transfer} reports gains on a matched 16-block
subset across three platforms; this evaluates transfer, not parity.

\subsection{Qualitative Cases}
\label{app:real-world-cases}

The outdoor comparison in \cref{fig:real-world-car-case} isolates the effect of
stale memory. \Cref{fig:real-world-cup-case} provides the corresponding indoor
pair: \evolvingworldnav{} routes directly to the cup's predicted office
location, whereas Last Seen spends its first inspection at the now-empty pantry
rack. These traces visualize representative outcomes and do not add observations
to the aggregate success-rate measurements.

\begin{figure}[H]
\centering
\begin{minipage}[t]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{figures/Real_World_Case_third_2.pdf}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{figures/Real_World_Case_third_3.pdf}
\end{minipage}
\caption{Indoor cup search with predictive belief (left) and stale Last Seen memory (right).}
\label{fig:real-world-cup-case}
\end{figure}

\paragraph{Additional system execution cases.}
Beyond predictive target search,
\cref{fig:real-world-case-indoor,fig:real-world-case-outdoor} document two
longer-horizon executions. The indoor
case combines navigation, person verification, document collection, and
delivery; the outdoor case shows obstacle detection and route replanning during
shared-bike search. They demonstrate broader system orchestration but are not
used as quantitative evidence for the predictive-belief module.

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{figures/Real_World_Case_1.pdf}
\caption{Indoor long-horizon execution with verification, collection, and delivery.}
\label{fig:real-world-case-indoor}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{figures/Real_World_Case_2.pdf}
\caption{Outdoor search with obstacle-triggered route replanning.}
\label{fig:real-world-case-outdoor}
\end{figure}

\FloatBarrier

\section{Reproducibility and Responsible AI}
\label{app:reproducibility}

\subsection{Reproducibility Checklist}
\label{app:protocol}

The completed release will report:
\begin{itemize}[leftmargin=5mm,itemsep=0.4mm]
    \item source dataset versions, licenses, mappings, filters, and canonical-record statistics;
    \item HSSD and Habitat versions, scene identifiers, object assets, placement checks, and NavMesh settings;
    \item split manifests, paired-example identifiers, checksums, and leakage-audit output;
    \item model width, layers, attention heads, time features, history length, optimizer, learning rate, epochs, seeds, and checkpoint rule;
    \item detector and depth inputs, visibility computation, candidate and action budgets, evidence likelihoods, and planner hyperparameters;
    \item hardware, wall-clock latency, memory growth, repeated-run aggregation, and confidence intervals; and
    \item reproduced versus quoted baseline values, grouped results, failure labels, and all component-aligned ablations.
\end{itemize}

\subsection{Ethics}

Persistent visual memory may capture people or private spaces, while an incorrect belief may induce unnecessary motion or unsafe behavior. Deployments should obtain consent, enforce retention and access controls, preserve provenance, and retain platform-specific collision avoidance and emergency stops outside the learned model. Our benchmark compiles existing records into simulated HSSD episodes and introduces no new human-subject study.

\subsection{AI Use}

Generative AI tools assisted manuscript editing, LaTeX checks, and local engineering. They did not generate benchmark observations, ground-truth labels, or reported scores. The authors reviewed all assisted material and remain responsible for the content and artifacts.
