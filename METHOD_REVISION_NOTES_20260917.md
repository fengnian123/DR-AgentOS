# Method 与 P4D-Bench 修改说明（2026-09-17）

本次修改针对 `sections/method.tex` 和 `sections/benchmark.tex`，编译后两节从第 4 页开始，在第 6 页中部结束，合计约 2.5 页。保留原方法、实验数值及三张图的内容，不调整 ICLR 模板、正文字号或页边距。

主要调整：

- 按“问题定义 → 因果记忆与历史编码 → persistence–relocation 信念 → 检查与更新”组织 Method，删除重复动机和流程总结。
- 主文保留结构化先验、成本感知检查、可见性加权更新三组公式；完整编码、pointer 评分及记忆形式化移入附录 A。
- 明确 `unknown` 的概率支持集、静态场景的逐步后验递推及在线场景的历史时间边界。Persistence 表示查询时位于最后观测位置，也允许离开后返回；不将其表述为“期间从未移动”。
- Benchmark 合并为“世界构建与因果观测”和“任务、协议与评估”两节。保留数据规模、来源、任务比例、配对控制和 N1–N5；生成统计、审计细则与完整协议表置于附录 B。
- 将两幅 benchmark 图并排排版，消除压缩后原环绕环境的碰撞。
- 提交前同步远端实验部分更新，并重新编译整篇论文。

原稿中仍需作者核实的内容：

1. `figures/P4D_Benchmark.pdf` 的任务分布图标注 EQA 43.36%，而正文四类任务比例为 prediction 16%、ObjectNav 27%、VLN 29%、EQA 28%。图中 N1–N5 与 EQA 标注比例合计 113.37%；N5 又被正文定义为跨场景评测维度。需确认图、正文是否使用不同统计口径，以及是否存在重叠计数。本次没有依据图或正文之一擅自改写另一方。
2. 在线动态场景尚未明确何时重新启用已检查位置，也未完整说明到达时间预测与已有后验的组合方式。本文保留原有“以最新因果历史预测、每个动作片段后重规划”的接口，未新增未经实现核实的机制。

验证：使用 `latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex` 编译，检查引用、公式、修改文件格式及第 4–6 页渲染；无未定义引用、未定义文献或 overfull box，保留其他章节原有的排版警告。此次为论文编辑；未重新运行实验，也不构成对现有实验数值的独立复核。
