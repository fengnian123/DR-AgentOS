# Method 恢复与重新修订（2026-09-17）

本次按作者更正后的要求撤回过度压缩：**Method 本身**从第 4 页延续到第 6 页中部，Benchmark 在其后展开。前一版将两节合并压缩至约 2.5 页，误解了篇幅范围；本次以压缩前的 `4e98374` 为内容基准恢复，并保留最新远端 `5b8fc4a` 的完整实验表格和实验修改。

## 内容恢复

- Method 原有 3 个子节、5 个技术段落、13 组展示公式和全部标签均保留在主文。
- 恢复导航目标、版本化记忆、目标历史、连续时间 token、Transformer、persistence/pointer/prior、训练损失、检查策略、负证据似然、后验更新和到达时间预测。
- Benchmark 六个子节、数据规模、生成统计、任务比例、审计说明及协议表恢复至压缩前内容。
- 撤销上次的正文到附录迁移，避免恢复后重复出现同一推导。
- Figure 3、4 恢复为各自独立的右侧 `0.58\textwidth` 环绕图，在第 7 页上下排列，均由说明文字环绕。增加分页空间检查，防止图片越过页底。

## 本次保留的适度修订

只精简重复措辞、补齐必要定义并统一符号。明确 `unknown` 的概率支持集、persistence 包括离开后返回、固定目标协议的逐步后验递推，以及在线历史的因果时间边界。保持原技术流程，不补写未经实现核实的新机制。未修改 ICLR 样式、正文字号或页边距。

## 验证

使用 `latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex` 编译，并检查第 4–8 页渲染。无未定义引用、未定义文献、overfull box 或 wrapfigure 碰撞；与压缩前逐项核对 Method 的结构和公式，以及 Benchmark 的完整正文。实验部分与最新远端一致。

## 原稿待核实事项

Figure 3 的任务比例与正文使用的比例仍不一致；在线动态场景中重新启用已检查候选的规则仍需明确。本次保留现有内容，未擅自改数字或推定实现。
